/*import { FormControl } from '@angular/forms';

export function validateNeedTotal(ctrl: FormControl) {

    const value = ctrl.value;
    const isNeedOngoing = true;
    console.log('calidateTotal control:', ctrl.root.parent);
    console.log('validateTotal isNeedOngoing:', isNeedOngoing);

    const valid = isNeedOngoing ? true : value > 10;

    return valid ? null : {
        validTotal: {
            valid: false
        }
    }
}*/

